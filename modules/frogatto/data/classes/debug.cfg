{/*
xywh: "overload(
	def(decimal x, decimal y, decimal w, decimal h) -> commands debug_rect(x,y,w,h),
	def([decimal] pos) -> commands debug_rect(pos[0],pos[1],pos[2],pos[3]),
)",
xyxy: "overload(
	def(decimal x, decimal y, decimal w, decimal h) -> commands debug_rect(x,y,w,h),
	def([decimal] pos) -> commands debug_rect(pos[0],pos[1],pos[2]-pos[0],pos[3]-pos[1]),
)"
*/}