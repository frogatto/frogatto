[prototype]
	prototype=ct-block
	[vars]
		group=['support']
	[/vars]
	
	[properties]
		#set_alpha = "def(val) map(objs, 'obj', set(obj.alpha, val+200 #Sort of nudge the alpha down a level so it matches the blocks below it, since it looks kind of funny otherwise.#)) where objs = vars.overlays + [self]"
		additionalPlacementRequirementsMet="def(location) ('ground' in groups or 'support' in groups) where groups = groups_at(location, [0,0,-1])"
	[/properties]
[/prototype]