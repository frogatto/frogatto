[prototype]
  prototype=cell_projectile

  timer_frequency=2

  on_collide_object_body="[
    if(collide_with=level.player,[
      fire_event('pickup'),
      die()
    ])
  ]"

  on_timer="if(distance(me,level.player) < level.player.vars.magnetism, send_to_coordinates(me,level.player.midpoint_x,level.player.midpoint_y,7*level.player.vars.magnetism))"
[/prototype]
