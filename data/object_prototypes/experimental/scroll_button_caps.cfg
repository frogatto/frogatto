[prototype]
	prototype = button_input
	zorder = 100
	
	on_create = "add(level.player.vars.widgets, [self])" #Override the set position behaviour of button - we don't need it now, since we're positioned by our parent.
	
	on_mouse_left_down = "[%PROTO%, parent.scroll_to(upside_down)]"
[/prototype]