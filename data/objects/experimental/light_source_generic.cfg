{
ignore_collide: true,
id: "light_source_generic",
is_strict: true,
zorder: "@include data/zorder.cfg:shadow",
activation_border: 0, #This should be whatever the flicker radius is, if it moves around.

editor_info: {
	category: "experimental",
},

properties: {
},

on_create: "[
	if(true, debug('cmd 1')),
	if(false, debug('cmd 2'); debug('cmd 3')),
	if(true, debug('cmd 4'); debug('cmd 5')),
]",

animation: {
	id: "normal",
	image: "experimental/shader-shadow-test/circular-32x32.png",
	rect: [0,0,31,31],
	frames: 1,
	duration: 100000,
	//blend:["src_alpha","one"],
	//blend: ["dst_color","zero"],
	//blend_equation:["reverse_subtract","reverse_subtract"]
	//blend:["src_alpha","one_minus_src_alpha"],

},
}
