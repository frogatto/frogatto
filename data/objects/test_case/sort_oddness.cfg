	[object_type]
	id=three_element_series_puzzle_controller
	ignore_collide=true
	activation_border=350
	zorder=5
	
	[editor_info]
	category=controllers
		[var]
		name=switch_one
		type=label
		[/var]
		[var]
		name=switch_two
		type=label
		[/var]
		[var]
		name=switch_three
		type=label
		[/var]
	[/editor_info]


	[vars]
		valid_list=[1,2,3]
		completed_list=[]
	[/vars]
	#hack to make it invisible in game, but visible in editor.
	#on_create="[set(alpha,0),
	#	get_object(level, switch_one)
	#	random sequence
	
	on_create="[set(valid_list, sort(valid_list, 1d2=2))]"
	on_process="[if(cycle=1,debug(valid_list))]"
	
	#trigger_event(name)
	# find name in list, set completed_list to name's index
	# if complete_list.size = 3, then compare lists.
	# 	if equal, fire trigger event.  if not, fire fail event, and reset list.
	 	
	 
	on_fail="[debug('sequence failed'),set(vars.completed_list, [])]"
	
	on_end_anim="animation('normal')"
		[animation]
		id=normal
		image=effects/particles.png
		x=86
		y=11
		w=28
		h=28
		frames=1
		duration=1000
		[/animation]

	[/object_type]
