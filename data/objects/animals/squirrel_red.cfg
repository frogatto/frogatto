	[object_type]
	id=squirrel_red
	prototype=ant
	solid_area=17,11,30,27
	[vars]
		points_value="223"
		turns_at_cliffs=1
		charge_up=0
		charge_time=6
		jump_clearance=100
		jump_distance=120
	[/vars]
	[editor_info]
		category=animals
		[var]
			name=charge_time
			value=6
		[/var]
	[/editor_info]
		[base:animation]
		image=enemies/squirrel-red.png
		rotate_on_slope=yes
		pad=3
		accel_x=0
		accel_y=80
		body_area=all
		[/animation]
		
		on_enter_walk_anim="[set(vars.charge_up, vars.charge_up+1), if(vars.charge_up >= vars.charge_time, animation('toss'))]"
		on_end_toss_anim="[set(vars.charge_up, 0), animation('walk')]"
		on_throw_acorn="spawn('acorn', midpoint_x + (20 * facing), y, facing, [animation('thrown'), set(velocity_x, velx),set(velocity_y, -700)])
		where velx = 500 * facing"

	on_timer="if(not (animation in ['thrown', 'on_back']), [set(rotate, slope_standing_on), if(turns_at_cliffs and is_standing and animation = 'walk' and distance_to_cliff < DISTANCE_TO_CLIFF_CHECK,
			#if we're near the end of a branch, see if there's a spot to jump up to#
			if(standable(level, front + facing*96, y-140, 1, 180), [debug('up!'),set(velocity_x,500*facing),set(velocity_y,-1800) ]  ,

				#there's not.  See if there's a place to jump forward to#
				if(standable(level, front + facing*160, midpoint_y, 1, 32), [debug('forward!'),set(velocity_x,1000*facing),set(velocity_y,-600) ] ,
				
					#there's not.  See if there's a place to jump down to#
					if(standable(level, front + facing*110, midpoint_y, 1, 160), [debug('down!'),set(velocity_x,400*facing),set(velocity_y,-800) ] ,
	
					[set(facing, -facing),animation('turn')]))))])"


		
		[animation]
		id=stand
		rect=1,1,45,29
		attack_area=26,18,35,28
		[/animation]
	
		[animation]
		id=thrown
		body_area=none
		rect=97,129,141,157
		solid_area=8,12,28,27
		[/animation]

		[animation]
		id=on_back
		rect=1,129,45,157
		solid_area=8,16,28,27
		[/animation]

		[animation]
		id=walk   #blink variant
		rect=1,33,45,61
		accel_x=250
		attack_area=26,18,35,28
		[/animation]

		[animation]
		id=walk
		accel_x=250
		rect=1,1,45,29
		attack_area=26,18,35,28
		[/animation]

		[animation]
		id=spring
		rect=1,65,45,93
		frames=1
		[/animation]

		[animation]
		id=turn
		rect=1,97,45,125
		frames=5
		attack_area=11,18,35,28
		[/animation]

		[animation]
		id=hurt
		rect=49,65,93,93
		[/animation]

		[animation]
		id=toss
		rect=1,161,45,189
		events="42:throw_acorn"
		duration=6
		frames=9
		frames_per_row=3
		[/animation]
	[/object_type]

