#This file should contain a mapping of strings -> integers. The strings are
#named zorders, to their corresponding integer values. There must be a 
#strict 1:1 correspondence of strings:integers.
{
}
