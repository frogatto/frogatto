{
regular_font: "string :: get()",
// bold_font: "string :: get(bold, regular)",
// italic_font: "string :: get(normal, italic)",
get: "def (string style = '') -> string
	if(query_cache(global_cache(), null, 'unifont.ttf' not in get_files_in_dir('data/fonts/')),
		'unifont.ttf',
		'unifont.ttf'
	)
",

get_font: "def (map dictionary = {}) -> string
	if(query_cache(global_cache(), null, 'unifont.ttf' not in get_files_in_dir('data/fonts/')),
		'unifont.ttf',
		'unifont.ttf'
	)
",
}
